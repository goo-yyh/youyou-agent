# Examples

当前示例：

- `minimal_agent.rs`
  - 展示 `ModelProvider`、`ToolHandler`、`SessionStorage`、`MemoryStorage` 的最小接入方式
  - 演示 `new_session -> send -> cancel -> resume -> close`

运行方式：

```bash
cargo run --example minimal_agent
```
